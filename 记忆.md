# 记忆 · AI / 新成员接手指南

> 新开对话或新人接手，**按阅读顺序走一遍**即可：项目是什么、原则是什么、进度在哪、代码去哪找。

---

## 阅读顺序（约 15～25 分钟）

| 顺序 | 文档 | 目的 |
|:----:|------|------|
| 1 | [README.md](README.md) | 一句话定位、快速启动、文档入口 |
| 2 | [docs/development-principles.md](docs/development-principles.md) | **开发底线**（怎么写、怎么验、什么不能做） |
| 3 | [docs/architecture.md](docs/architecture.md) | **CODEMAP**：包边界、依赖、改代码去哪 |
| 3.5 | [docs/codebase-guide.md](docs/codebase-guide.md) | **代码库导读**：分层全景与横切关注点 |
| 4 | [internal/README.md](internal/README.md) | **改 X 找哪**（任务导向 FAQ） |
| 5 | [docs/dev-log.md](docs/dev-log.md) | **唯一进度来源**（以最新一条为准） |
| 6 | [docs/deploy.md](docs/deploy.md) | 部署 / 配置 / 验收（改网络或配置时必读） |
| 7 | [scripts/README.md](scripts/README.md) | 本机构建与发版脚本 |
| 8 | [docs/versioning.md](docs/versioning.md) | 版本规则（**AI 禁止改 `VERSION`**） |

按需：

| 场景 | 文档 |
|------|------|
| 流量/路由走向（分流·托管·OpenVPN 对照） | [docs/traffic-routing.md](docs/traffic-routing.md) |
| 生产上线前 | [docs/security-hardening.md](docs/security-hardening.md) |
| 现场排障 | [docs/troubleshooting.md](docs/troubleshooting.md) |
| 注释怎么写 | [docs/comment-style.md](docs/comment-style.md) |
| 商用授权 | [docs/licensing.md](docs/licensing.md) |
| 文档总索引与放置规则 | [docs/README.md](docs/README.md) |
| v1.0 功能与 step 规划（存档） | [docs/archive/meta-plan.md](docs/archive/meta-plan.md) |
| 手机客户端实施蓝图（代码未开工） | [docs/plans/mobile-client-plan.md](docs/plans/mobile-client-plan.md) |

---

## 当前阶段（只记「现在」）

| 项 | 状态 |
|----|------|
| 产品 / 模块 | **HaoVPN** / `haovpn`；当前版本见根目录 [`VERSION`](VERSION)（撰写时 **0.1.2**） |
| v1.0 step1～10 + 账号合一 + B 档加固 | ✅ 已完成（细节见历史 [dev-log](docs/dev-log.md)） |
| WebUI favicon + 手动封禁时长 | ✅ 见 dev-log 2026-08-31 |
| 封禁豁免 + 客户端封禁提示 | ✅ 见 dev-log 2026-08-31 |
| 架构解耦第 1～19 轮 | ✅ 已完成；**CODEMAP 以 [architecture.md](docs/architecture.md) 为准** |
| **step11 field 硬门禁** | ⏳ `.\scripts\dev-field-gate.ps1 -PlcHost <IP> -UseHomeConfig` 须 0 FAIL |
| **step11.12 重启自连** | ⏳ 手工确认后写入 dev-log |

**下一步（管理员 + 工控网）**：

```powershell
.\scripts\dev-field-gate.ps1 -PlcHost 192.168.1.10 -UseHomeConfig
```

field 全绿后：在 `docs/dev-log.md` 记日期与 PlcHost，并把上表 step11 标 ✅。

> 历史轮次、功能迭代过程**不要**在本文件堆长表；一律查 [dev-log.md](docs/dev-log.md)。

---

## 接手后第一件事

1. 读完上表 1～5。
2. 打开 [docs/dev-log.md](docs/dev-log.md) **最新一条**，确认无冲突中的工作项。
3. 改代码前对照 [architecture.md](docs/architecture.md) / [internal/README.md](internal/README.md)。
4. 遵守 [development-principles.md](docs/development-principles.md)：**不猜测、不糊弄**；不确定就写测试或加日志。
5. 完成一块工作后：更新 `dev-log`；若影响部署/配置/安全行为，同步 `deploy.md` / `security-hardening.md` / `troubleshooting.md`。

---

## 关键约定

- **安全 > 简单 > 快**，不为快牺牲安全。
- 管理口默认 `127.0.0.1` + TUN IP；绑 `0.0.0.0` 须 **`api.allow_public_bind: true`** 且自担风险。
- 配置 **YAML**；持久化 **SQLite**（不是 JSON）。
- 全平台：Linux / Windows / macOS；服务端 + 客户端（GUI 发版以 Windows 为主）。
- **版本**：仅根目录 `VERSION`，**仅开发者可改；AI 禁止修改**。发版要点写 **dev-log**（勿在 docs 堆 release-notes DRAFT）。
- **Git**：AI 不得擅自 commit/push；禁止 Co-authored-by / AI 署名。
- **环境**：Windows + **pwsh 7** + **Go 1.26**；本机 `.\scripts\build-local.ps1`，发版 `.\scripts\build-release.ps1`。
- **Windows 提权**：TUN/服务等可用 `sudo` 或管理员终端；观测优先读 `*.live.log`。

---

## 目录速查

```
HaoVPN/
├── VERSION                 # 唯一版本号（仅开发者可改）
├── README.md               # 简介与快速开始
├── 记忆.md                 # 本文件（接手入口）
├── docs/                   # 活文档平铺 + 分级子目录（见 docs/README.md）
│   ├── plans/              # 未开工蓝图（如手机）
│   └── archive/            # 存档（meta-plan、code-audit）
├── web/                    # WebUI 模板 + static/*.js（go:embed）
├── scripts/                # 构建与验收脚本（见 scripts/README.md）
│   ├── platforms.txt
│   └── lib/build-common.ps1
├── bin/                    # build-local 输出
├── dist/                   # build-release 输出
├── cmd/server|client|client-gui
└── internal/               # 核心包（见 internal/README.md、docs/architecture.md）
```

包级导航以 **architecture.md CODEMAP** 为准，此处不重复列全包名。

---

*最后更新：2026-08-31 · 架构解耦第十九轮*
