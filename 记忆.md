# 记忆 · AI / 新成员接手指南

> 每次新开对话或新人接手，**按下面顺序阅读**，即可知道项目是什么、原则是什么、做到哪了、下一步该干什么。

---

## 阅读顺序（约 15～30 分钟）

| 顺序 | 文档 | 目的 |
|:----:|------|------|
| 1 | [README.md](README.md) | 项目一句话、快速用法、文档索引 |
| 2 | [docs/development-principles.md](docs/development-principles.md) | **开发底线**：怎么写代码、怎么验证、什么绝对不能做 |
| 3 | [docs/meta-plan.md](docs/meta-plan.md) | v1.0 全貌：功能清单、目录结构、**开发 step 顺序** |
| 3.5 | [docs/architecture.md](docs/architecture.md) | **CODEMAP**：包边界、依赖规则、改代码去哪找 |
| 3.55 | [internal/README.md](internal/README.md) | **包索引**：改 X 功能来哪、分层速览 |
| 3.6 | [docs/comment-style.md](docs/comment-style.md) | **注释规范**：导出符号、字段、流程阶段 |
| 4 | [docs/dev-log.md](docs/dev-log.md) | **当前进度**、已踩的坑、未完成项（以最新一条为准） |
| 5 | [docs/deploy.md](docs/deploy.md) | 部署拓扑、配置、验收测试（改网络/配置相关时必读） |
| 6 | [scripts/README.md](scripts/README.md) | 打包与本地测试脚本怎么用 |
| 7 | [docs/versioning.md](docs/versioning.md) | **版本管理**（AI 禁止改 VERSION） |

按需查阅：

- 生产上线前 → [docs/security-hardening.md](docs/security-hardening.md)
- 现场出问题 → [docs/troubleshooting.md](docs/troubleshooting.md)

---

## 当前阶段（手动维护，完成一步改一步）

| 项目 | 状态 |
|------|------|
| 规划文档 | ✅ 已完成 |
| 文档体系 / 脚本骨架 | ✅ 已完成 |
| **step1～10** | ✅ 已完成 |
| **v1.0 收尾 P0**（AES 私钥 / 防重放 / IP 池持久化 / peer 隔离） | ✅ 已完成 + 单测 |
| **v1.0 收尾 P1**（zip 导出 / 日志快照 / 权限 WARN / reconnect / macOS NAT 诚实） | ✅ 已完成 |
| **VPN 账号合一**（users 物理合并 peers、握手下发策略、三种 IP 模式、入站校验、WebUI 合一） | ✅ 已完成 + `go test ./...` |
| **step11 smoke** | ✅ `dev-acceptance`（无 TUN，仅 API/安全） |
| **B 档全面加固**（RBAC / TLS / DNS / 杀开关 / 租约 IP / WebUI 维护） | ✅ 已完成 + `go test ./...` |
| **B 档后审计修复**（DPAPI LocalMachine / IP 池全恢复 / 僵尸会话 / DNS Restore / TOCTOU / 导出与 CA / WFP 杀开关） | ✅ 已完成 + `go test ./...` |
| **B 后原则审计修复**（隧道 must_change / 杀开关无泄漏 / WFP 全量子层清理 / GoSafe / DNS 平台诚实 / 中文错误） | ✅ 已完成 + `go test ./...` |
| **管理控制台性能**（日志历史库、分页筛选、N+1 修复、数据保留） | ✅ 已完成 + `go test ./...` + build-local |
| **HaoVPN 首版**（模块 haovpn、品牌/二进制重命名、gitignore、文档治理） | ✅ 已完成 + `go test ./...` + build-local |
| **架构解耦第三轮**（Server ApplyDefaults、netutil 网关、删死代码、cmd 收敛） | ✅ 已完成 + 单测 |
| **全量注释补全**（comment-style、Tier1–4 中文 godoc、web/README） | ✅ 已完成 + `go test ./...` |
| **架构解耦第四轮**（netutil 收敛、api/clientapp/transport 拆分、readmodel/fileutil、注释与 CODEMAP） | ✅ 已完成 + `go test ./...` + build-local |
| **架构解耦第五轮**（paginate、persist 辅助、vpnaccount DeleteAccount、maintenance、api 去 ippool、clientgui/clientapp 收敛、注释扩展） | ✅ 已完成 + `go test ./...` + build-local |
| **架构解耦第六轮**（Wintun 日志/孤儿清理、管理员改密、删 DB 迁移、paginate ParseIntDefault） | ✅ 已完成 + `go test ./...` + build-local |
| **架构解耦第七轮**（原子写/timeutil/导出 YAML/helper 统一/胖文件拆分/CODEMAP） | ✅ 已完成 + `go test ./...` + build-local |
| **架构解耦第八轮**（叶子工具/领域边界/monitor JOIN/胖文件拆分/doc.go/全局文档） | ✅ 已完成 + `go test ./...` + build-local |
| **架构解耦第十轮**（persist 分页、serverapp 分阶段、安全审计闭环、文档治理、LICENSE 法律层） | ✅ 已完成 + `go test ./...` + build-local + smoke |
| **架构解耦第十一轮**（releaseDynamicIP、审计补测、文档/发版授权清单） | ✅ 已完成 + `go test ./...` + build-local + smoke + security-check |
| **探针防御 + 登录体验**（reject_second、GUI 等握手、CLI 无回显、security_events/ip_blocks） | ✅ 已完成 + `go test ./...` + build-local |
| **探针审计修复**（*bool 哨兵、超时不计探针、OnDataplaneFailed、特征中文、hardening 对照表） | ✅ 已完成 + 相关包单测 + build-local |
| **架构解耦第十二轮**（叶子工具、鉴权哨兵、封禁始终挂 Probe、握手回滚、改密旧密码、双 lockout、明文钥拒绝） | ✅ 已完成 + `go test ./...` + build-local |
| **架构解耦第十三轮**（netutil 收口、ExitLAN via 门禁、管理面超时/导出 POST+CSRF、审计中文、文档） | ✅ 已完成 + `go test ./...` + build-local |
| **架构解耦第十四轮**（netutil LAN/CIDR、auth 哨兵、health/末管理员/logout、API 辅助、GUI eng 锁） | ✅ 已完成 + 相关包单测 + build-local |
| **Peer 路由 + 重连续算 + 托盘 Logo**（对齐 ZeroTier Managed Routes） | ✅ 已完成 + `go test ./...` + build-local |
| **互访 hub 直转 + 去冗余 /32 + 应用生效** | ✅ 已完成（须重启 server；改 peers 后点应用生效） |
| **客户端去网关 /32 + GUI 重连 failFast 修复** | ✅ 已完成（须更新客户端） |
| **本地网段注册 + 托管解耦 + via 出口** | ✅ 已完成 + 相关包单测 + build-local（须更新服务端与客户端；via 机配 `local_lans`） |
| **半死会话顶替 + account_online 持续重试** | ✅ 已完成 + 单测 + build-local（须同时更新 server 与 client） |
| **via 回程 ExitLANs + 伪造源刷屏治理** | ✅ 已完成 + 单测（须同时更新 server 与 client；local_lans 勿与服务端 NAT 重叠） |
| **ICS SkipAsSource 根因修复（保留 ICS）** | ✅ 已完成（禁 SkipICS；ICS 后非 VPN 地址 SkipAsSource + 重装路由） |
| **托管路由 UI：应用 loading + 访问方多选弹窗** | ✅ 已完成（静态资源，重启/刷新控制台即可） |
| **客户端差分重连 + GUI 日志 300 行** | ✅ 已完成 + 单测（临时断线保留数据面；须更新客户端） |
| **GUI 登出/重连不卡 UI（ICS 后台清理）** | ✅ 已完成 + 单测（须更新客户端） |
| **via/ICS 前推迟装路由** | ✅ 已完成 + 单测（避免装路由两次） |
| **发送队列可配 + WebUI 展示时区 + defaults 全注释** | ✅ 已完成 + 相关包单测 + build-local |
| **托盘本机路由分栏 + README 品牌** | ✅ 已完成 + clientgui 单测 + build-local（须更新 GUI） |
| **GUI 托盘配置：自动连接/自启/异步退出** | ✅ 已完成 + 单测 + build-local（须更新 GUI） |
| **step11 field 硬门禁** | ⏳ `dev-field-gate.ps1 -PlcHost <IP>` 须 0 FAIL（TUN/NAT/PLC/服务） |
| **step11.12 重启自连** | ⏳ 手工确认后写入 dev-log |

**下一步（管理员 + 工控网）**：

```powershell
.\scripts\dev-field-gate.ps1 -PlcHost 192.168.1.10 -UseHomeConfig
```

field 全绿后，在 `docs/dev-log.md` 记录日期与 PlcHost，并将 step11 标 ✅。

---

*最后更新：2026-08-30 · 文档标注 Linux/macOS 自启未实现*


## 接手后第一件事

1. 读完上表 1～4 号文档。
2. 打开 [docs/dev-log.md](docs/dev-log.md) 看最后一条日志，确认没有别人正在做的冲突项。
3. 对照 [docs/meta-plan.md](docs/meta-plan.md) 找当前 step 的**具体文件清单**。
4. 开发时遵守 [docs/development-principles.md](docs/development-principles.md)：**不猜测、不糊弄**；不确定就写测试或加日志验证。
5. 完成一个 step 后：更新 `docs/dev-log.md`；若改动了部署或配置行为，同步更新 `docs/deploy.md`。

---

## 关键约定（别搞错）

- **安全 > 简单 > 快**，不为快牺牲安全。
- 管理口默认只绑 `127.0.0.1` + TUN IP；绑 `0.0.0.0` 必须 **`api.allow_public_bind: true`** 且自担风险。
- 配置格式：**YAML**；持久化：**SQLite**（不是 JSON）。
- 全平台：Linux / Windows / macOS，服务端 + 客户端。
- **版本**：仅根目录 `VERSION` 文件，**仅开发者可改，AI 禁止修改**。
- **Git**：AI 不得擅自 commit/push；提交者只能是开发者本人，**禁止 Co-authored-by / 协助者**。
- **环境**：Windows + **pwsh 7** + **Go 1.26**；发版用 `.\scripts\build-release.ps1`。
- **Windows 提权**：TUN 等需要管理员时可用 `sudo` 或提权终端；**观测优先读 `*.live.log`**（不依赖控制台）。
- 只提交用户要求的改动；**不要擅自 git commit**。

---

## 目录速查

```
HaoVPN/
├── VERSION                # 唯一版本号（仅开发者可改，AI 禁止动）
├── README.md              # 项目简介
├── 记忆.md                # 本文件
├── docs/                  # 所有详细文档（含 architecture、comment-style）
├── web/                   # WebUI 模板与静态资源（go:embed）
├── scripts/               # 打包 & 开发测试脚本
│   ├── platforms.txt      # 交叉编译平台列表
│   └── lib/build-common.ps1
├── bin/                   # build-local 本机构建输出
├── dist/                  # build-release 全平台产物
├── cmd/server/            # 服务端入口 → serverapp
├── cmd/client/            # CLI 客户端 → clientapp
├── cmd/client-gui/        # 桌面入口 → clientgui（UI 在 internal/clientgui）
└── internal/              # 核心模块（见 internal/README.md、docs/architecture.md）
    ├── clientapp/ clientgui/ serverapp/
    ├── paginate/ maintenance/ timeutil/ fileutil/
    ├── netutil/ winnet/ readmodel/
    ├── api/ vpnaccount/ tunnel/ transport/
    └── netstack/ tun/ persist/ auth/ sessionmgr/ …
```

---

*最后更新：2026-08-30 · 文档标注 Linux/macOS 自启未实现*

